- hosts:
     localhost
  vars:
     app_name: plusdata
     github_user: GeoNode
     server_name: 10.116.2.220
     deploy_user: plusdata-prod
     virtualenv_dir: /webapps/{{deploy_user}}/venvs
     app_code_dir: /webapps/{{deploy_user}}
  roles:
  - ansible-geonode
